## 概要
- `typescript` を `^5.2.2` から `^6.0.3` へ更新しました。
- `react-hooks/exhaustive-deps` の warning を解消し、lint が通るようにしました。
- `package-lock.json` を更新しました。

## 変更ログと Breaking Changes の確認
- TypeScript 6.0 のリリースノートを確認しました。
- 主な破壊的変更として、TypeScript API 周りの削除や ES2020 / Node 14.17+ 前提化がありました。
- このリポジトリは TypeScript をビルド用途で利用しており、独自に TypeScript Compiler API を使っていません。
- GitHub Actions は Node 16 で動作しており、今回の更新範囲では影響は限定的と判断しました。

## 検証
- `npm test`
  - `test` スクリプト未定義のため、テストコードなしとして扱いました。
- `npm run build`
  - 成功
- `npm run lint`
  - 成功
